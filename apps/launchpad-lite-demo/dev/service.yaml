apiVersion: v1
kind: Service
metadata:
  name: tekton-hello
  labels:
    app.kubernetes.io/name: tekton-hello
    app.kubernetes.io/part-of: launchpad-lite
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tekton-hello
  ports:
    - name: http
      port: 80
      targetPort: http
